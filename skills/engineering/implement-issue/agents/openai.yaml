interface:
  display_name: "Implement Local Issue"
  short_description: "Implement and verify one ready local issue"
  default_prompt: "Use $implement-issue to implement this ready local issue through testing and review."

policy:
  allow_implicit_invocation: false
