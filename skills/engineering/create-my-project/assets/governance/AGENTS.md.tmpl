# Project

{{PROJECT_DESCRIPTION}}

## Tooling

- Profile: `{{PROFILE}}`
- Install: `{{INSTALL_COMMAND}}`
- Develop: `{{DEV_COMMAND}}`
- Test: `{{TEST_COMMAND}}`
- Build: `{{BUILD_COMMAND}}`

## Project governance

- Keep architecture facts in `docs/agents/architecture.md`.
- Keep development commands in `docs/agents/development.md`.
- Keep test strategy in `docs/agents/testing.md`.
- Record lasting architecture decisions under `docs/adr/`.

## Global constraints

- Preserve public APIs and persisted data unless a breaking change is explicitly approved.
- Do not add dependencies without explaining the need and obtaining approval.
- Keep changes scoped to the requested task.
- Reuse existing project patterns before adding abstractions.
- Run relevant tests and build checks after code changes.
- Update governance documents only when verified project facts change.
